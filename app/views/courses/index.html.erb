<div class="relative bg-gray-50 pt-8 pb-8 px-4 sm:px-6 lg:px-8">
  <div class="absolute inset-0">
    <div class="bg-white h-1/3 sm:h-2/3"></div>
  </div>
  <div class="relative max-w-7xl mx-auto">
    <div class="text-center">
      <h2 class="text-3xl tracking-tight font-extrabold text-gray-900 sm:text-4xl">
        Our Courses
      </h2>
      <p class="mt-3 max-w-2xl mx-auto text-xl text-gray-500 sm:mt-4">
        Learn from sequences curated by topic experts and a group of equally motivated peers. Tackle challenges to proceed through levels and build a portfolio of accomplishments.
        <%= link_to 'How it works', intro_courses_path, class: "text-indigo-500 hover:text-indigo-700 underline" %>.
      </p>
    </div>
    <div class="mt-12 max-w-lg mx-auto grid gap-5 lg:grid-cols-3 lg:max-w-none">
      <%= render partial: "course", collection: Course.listed_courses(current_user) %>
    </div>
  </div>
</div>

