<%= form_with(model: user_topic, local: true) do |form| %>
  <% if user_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_topic.errors.count, "error") %> prohibited this user_topic from being saved:</h2>

      <ul>
      <% user_topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
