<div class="max-w-sm bg-white shadow-lg rounded-lg overflow-hidden">
    <% if @person.image_url.present? %>
        <img src="<%= @person.image_url %>" alt="<%= @person.name %>" class="w-full h-56 object-cover object-center">
    
        <div class="flex items-center px-6 py-3 bg-gray-900"><svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 fill-current text-white">
                <path fill-rule="evenodd" clip-rule="evenodd" d="M17 21C15.8954 21 15 20.1046 15 19V15C15 13.8954 15.8954 13 17 13H19V12C19 8.13401 15.866 5 12 5C8.13401 5 5 8.13401 5 12V13H7C8.10457 13 9 13.8954 9 15V19C9 20.1046 8.10457 21 7 21H3V12C3 7.02944 7.02944 3 12 3C16.9706 3 21 7.02944 21 12V21H17ZM19 15H17V19H19V15ZM7 15H5V19H7V15Z"></path>
            </svg>
            <h1 class="mx-3 text-white font-semibold text-lg">Focusing</h1>
        </div>
    <% end %>
    <div class="py-4 px-6">
        <h1 class="text-xl font-semibold text-gray-800"><%= @person.name %> <% if @person.kind %>(<%= @person.kind %>)<% end %></h1>
        
        <% if current_user.try(:score).to_i > 500 %><%= link_to "Edit", edit_person_path(@person), class: "text-blue-500 underline" %><% end %>
        <p class="py-2 text-gray-700"><%= @person.description %></p>

        <% if @person.website.present? %>
            <%= link_to "Website", @person.website, class: "text-blue-500 underline mr-2" %>
        <% end %>

        <% if @person.twitter_url.present? %>
            <%= link_to "Twitter", @person.twitter_url, class: "text-blue-500 underline mr-2" %>
        <% end %>

        <% if @person.goodreads_url.present? %>
            <%= link_to "GoodReads Author Profile", @person.goodreads_url, class: "text-blue-500 underline mr-2" %>
        <% end %>
    </div>

    <div class="py-4 px-6">
        <h2 class="text-md font-semibold text-gray-800">Works:</h2>
        <ul>
        <% @person.items.group_by(&:item_type).each do |it, items| %>
            <% items.each do |i| %>
                <li><%= render i %></li>
            <% end %>
        <% end %>
        </ul>
    </div>
</div>