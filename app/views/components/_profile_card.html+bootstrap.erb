	<div class="col-sm-6 col-xl-3 mb-4">
		<div class="card">

            <a class="list-group-item list-group-item-action" href="<%= link %>">
                    <div class="media">
                        <img class="u-avatar rounded-circle mr-3" src="<%= avatar_image %>" alt="<%= name %>">

                        <div class="media-body">
                            <div class="align-items-center">
                                <h4 class="mb-1">
                                    <b><%= name %></b>
                                </h4>
                                <p><%= description %></p>
                            </div>
                        </div>
                    </div>
            </a>

		</div>
	</div>